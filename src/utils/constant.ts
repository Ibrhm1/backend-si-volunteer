export enum ROLES {
  ADMIN = 'admin',
  MEMBER = 'member',
  ORGANIZER = 'organizer',
}

export enum TYPE {
  MEMBER = 'member',
  ORGANIZER = 'organizer',
}

export enum STATUS {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}
